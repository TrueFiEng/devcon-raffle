{
  "name": "devcon-raffle",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "lint": "wsrun -c lint",
    "build": "wsrun -te -c build",
    "test": "wsrun -c test",
    "verify": "wsrun -m -c verify",
    "update": "git submodule update --remote"
  },
  "workspaces": [
    "packages/contracts",
    "packages/frontend"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
