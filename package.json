{
  "name": "devcon-raffle",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "lint": "wsrun -c lint",
    "build": "wsrun -te -c build",
    "test": "wsrun -c test",
    "update": "git submodule update --remote"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "wsrun": "^5.2.4"
  }
}
